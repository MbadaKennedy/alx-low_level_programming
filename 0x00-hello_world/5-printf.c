#include <stdio.h>

/**
 * main - A C program that prints a string on execution
 *
 * Description: main - Prints out a string to studio.
 * Return: 0 if success
 **/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
